# Optional: use Python chat API. Point to the FastAPI server URL (no trailing slash).
# NEXT_PUBLIC_CHAT_API_URL=http://localhost:8000

# Optional (Python API): base URL of this portfolio site so the bot fetches latest context from /api/portfolio-context.
# PORTFOLIO_CONTEXT_URL=http://localhost:3000

# Optional: for richer chat answers (works in both Python and Next.js), add your OpenAI API key.
# OPENAI_API_KEY=sk-...
